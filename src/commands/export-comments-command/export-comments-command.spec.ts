import { ExportCommentsCommand } from './export-comments-command';

describe('ExportCommentsCommand', () => {
  it('should be defined', () => {
    expect(new ExportCommentsCommand()).toBeDefined();
  });
});
