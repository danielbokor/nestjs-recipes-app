import { SeedCommentsCommand } from './seed-comments-command';

describe('SeedCommentsCommand', () => {
  it('should be defined', () => {
    expect(new SeedCommentsCommand()).toBeDefined();
  });
});
